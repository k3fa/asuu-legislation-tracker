POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
# Use localhost for local development. For Railway, replace with your database host.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=asuu_tracker
# Optionally provide a full connection string instead.
# Example: postgresql://user:password@host:port/dbname
# On Railway set this variable to the connection string provided in the dashboard.
# DATABASE_URL=
